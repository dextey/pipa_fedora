# hyprlock — save as ~/.config/hypr/hyprlock.conf — Nord

general {
    # Don't try to authenticate an empty password (avoids spurious fails).
    ignore_empty_input = true
}

auth {
    # No fingerprint reader on the pipa.
    fingerprint:enabled = false
}

background {
    monitor =
    path = ~/.config/hypr/wallpaper.png
    blur_passes = 2
    blur_size = 5
    color = rgb(2e3440)
}

input-field {
    monitor =
    size = 320, 56
    position = 0, -40
    halign = center
    valign = center

    outline_thickness = 2
    rounding = 12
    dots_size = 0.3
    dots_spacing = 0.3

    outer_color = rgb(88c0d0)
    inner_color = rgb(3b4252)
    font_color  = rgb(eceff4)
    check_color = rgb(a3be8c)
    fail_color  = rgb(bf616a)

    placeholder_text = <i>Password…</i>
    fail_text = <i>$FAIL ($ATTEMPTS)</i>
    fade_on_empty = false
}

label {
    monitor =
    text = $TIME
    font_size = 64
    font_family = JetBrainsMono Nerd Font
    color = rgb(eceff4)
    position = 0, 120
    halign = center
    valign = center
}

label {
    monitor =
    text = cmd[update:60000] date +"%A, %d %B"
    font_size = 22
    font_family = JetBrainsMono Nerd Font
    color = rgb(d8dee9)
    position = 0, 50
    halign = center
    valign = center
}
